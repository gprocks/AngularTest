import { TeamTranslatorPipe } from './team-translator.pipe';

describe('TeamTranslatorPipe', () => {
  it('create an instance', () => {
    const pipe = new TeamTranslatorPipe();
    expect(pipe).toBeTruthy();
  });
});
